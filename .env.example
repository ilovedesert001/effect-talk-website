# Copy to .env.local and replace placeholder values with real secrets. Do not commit .env.local.
# See docs/env.md. Required for local dev and for CLI deploy (vercel / vercel --prod).

BACKEND_API_BASE_URL=http://localhost:4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/effecttalk

WORKOS_API_KEY=sk_test_xxx
WORKOS_CLIENT_ID=client_xxx
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback
WORKOS_COOKIE_PASSWORD=xxx

RESEND_API_KEY=re_xxx
API_KEY_PEPPER=change-me-in-production

APP_BASE_URL=http://localhost:3000
APP_ENV=local

NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
